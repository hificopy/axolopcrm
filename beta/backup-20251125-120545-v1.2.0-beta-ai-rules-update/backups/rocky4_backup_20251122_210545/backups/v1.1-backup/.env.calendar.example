# Google Calendar API Configuration
#
# To get these credentials:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable Google Calendar API
# 4. Go to Credentials → Create Credentials → OAuth 2.0 Client ID
# 5. Choose "Web Application"
# 6. Add authorized redirect URIs (see below)
# 7. Copy Client ID and Client Secret

# Google Calendar OAuth Credentials
GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# OAuth Redirect URI
# Development:
GOOGLE_REDIRECT_URI=http://localhost:3001/api/calendar/google/oauth/callback

# Production (update when deploying):
# GOOGLE_REDIRECT_URI=https://yourdomain.com/api/calendar/google/oauth/callback

# IMPORTANT: Add these redirect URIs in Google Cloud Console:
# - http://localhost:3001/api/calendar/google/oauth/callback (for development)
# - https://yourdomain.com/api/calendar/google/oauth/callback (for production)
